#include <iostream>
#include <string.h>
using namespace std;

int main(){
    int i;
    string  str,substr;
    while (getline(cin, str), getline(cin, substr)){
        for(i = 0;i < str.length();i++){
            if (substr.substr(i)) {
                
            }
        }
    }

    return 0;
}