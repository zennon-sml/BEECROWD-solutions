#include <bits/stdc++.h>

using namespace std;
int main(){
	int x,y,i,z=0;
	scanf("%d%d",&x,&y);
	for(i=1;1+y>i;i++){z++;
		printf("%d",i);
		if(z!=x){ printf(" "); }
		else{ printf("\n");z=0; }
		}
return 0;
}
