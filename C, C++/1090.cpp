#include <iostream>
#include <string.h>

using namespace std;
int main(){
    int i, n, z;
    string carta[1000000];
    while(cin >> n and n != 0){
        for(i = 0;i < n;i++){
            cin >> carta[i] >> carta[i+1];
        }
        z = carta.length()
    }

    return 0;
}